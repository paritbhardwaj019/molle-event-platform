-- This migration syncs the database schema with the Prisma schema
-- The actual changes were already applied via 'prisma db push'
-- This migration serves to mark the current state as migrated

-- Add adminWallet field to users table (already exists)
-- Add tickets table and TicketStatus enum (already exists)
-- This is a no-op migration to sync the migration history

-- No changes needed as schema is already in sync 